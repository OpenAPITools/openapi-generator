{{#emitJSDoc}}
  /**
   * Enum class {{classname}}.
   * @enum {{=<% %>=}}{<%datatype%>}<%={{ }}=%>
   * @readonly
   */
{{/emitJSDoc}}
  var exports = {
  {{#allowableValues}}
  {{#values}}
{{#emitJSDoc}}
    /**
     * value: {{{.}}}
     * @const
     */
{{/emitJSDoc}}
    "{{{.}}}": "{{{.}}}"{{^-last}},
    {{/-last}}
  {{/values}}
  {{/allowableValues}}
  };

  return exports;
}));
