{-|
Module : {{title}}
-}

module {{title}}
  ( module {{title}}.Client
  , module {{title}}.API
  , module {{title}}.Model
  , module {{title}}.MimeTypes
  , module {{title}}.Lens
  ) where

import {{title}}.API
import {{title}}.Client
import {{title}}.Model
import {{title}}.MimeTypes
import {{title}}.Lens
