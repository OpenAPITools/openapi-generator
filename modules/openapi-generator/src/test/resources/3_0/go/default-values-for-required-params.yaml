openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
paths: {}
components:
  schemas:
    foo:
      type: object
      properties:
        # bar is required but has a default value
        bar:
          type: string
          enum:
            - value1
          default: value1
        # baz is required but has a default value
        baz:
          type: string
          default: value2
        # qux is required but has no default value
        qux:
          type: string
        # thud is not required and has a default value
        thud:
          type: integer
          default: 42
      required:
        - bar
        - baz
        - qux