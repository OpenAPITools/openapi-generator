openapi: 3.0.1
info:
  title: SSE test
  description: SSE test cases
  version: 1.0.0
servers:
  - url: 'https://'
tags:
  - name: sse
paths:
  # Generator detects this as SSE, which is ok in the first place, but it cannot generate correct code for this case, thus it should throw an exception with an error message, like for notSse4
  # Code looks like this is tried but fails in this case
  /path/sse:
    get:
      operationId: sse
      responses:
        '200':
          description: multiple media types defined
          content:
            application/json:
              schema:
                type: string
            text/event-stream:
              schema:
                type: array
                format: event-stream
                items:
                  type: string
