openapi: 3.0.3
info:
  title: GraphQL NodeJS Express problematic example
  version: 1.0.0
paths:
  /items:
    get:
      tags:
        - test
      operationId: listItems
      parameters:
        - name: filter
          in: query
          schema:
            type: string
            example: |-
              line "quoted" \ path
              next
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: string
