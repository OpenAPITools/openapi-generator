openapi: 3.0.0
info:
  version: 1.0.0
  title: Binary response
paths:
  /limits:
    get:
      operationId: binaryResponse
      responses:
        '200':
          description: OK
          content:
            application/zip:
              schema:
                type: string
                format: binary