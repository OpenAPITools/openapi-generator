openapi: 3.0.2
info:
  title: foo api
  version: v1
  description: 'bar'

paths:
  '/users':
    get:
      summary: user list
      operationId: userList
      parameters:
        - name: mapBean
          in: query
          required: false
          description: mapBean
          style: deepObject
          explode: true
          schema:
            type: object
            properties:
              keyword:
                title: keyword
                type: string
      responses:
        '200':
          description: ok