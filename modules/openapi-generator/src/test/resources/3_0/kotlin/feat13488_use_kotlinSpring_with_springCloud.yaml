openapi: "3.0.1"
info:
  title: test
  version: "1.0"

paths:

  /api/v1/test/{id}:
    get:
      tags: [Test v1]
      operationId: test1
      parameters:
        - name: id
          in: path
          schema:
            type: string
      responses:
        200:
          description: OK