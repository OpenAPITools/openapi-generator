openapi: 3.0.0
info:
  title: Invalid x-jackson-json-setter-nulls value test
  version: 1.0.0
paths:
  /test:
    post:
      operationId: testPost
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestModel'
      responses:
        '200':
          description: OK
components:
  schemas:
    TestModel:
      type: object
      properties:
        bad:
          type: string
          nullable: false
          x-jackson-json-setter-nulls: BOGUS
