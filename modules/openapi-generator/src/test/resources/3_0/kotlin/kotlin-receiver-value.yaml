openapi: 3.0.3
info: {title: ReceiverValue, version: 1.0.0}
paths:
  /items:
    get:
      operationId: listItems
      parameters:
        - name: value
          in: query
          schema: {type: string}
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: array, items: {type: string}}
  /lookup:
    get:
      operationId: lookupItems
      parameters:
        - name: value
          in: query
          schema: {type: string}
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema: {type: object, additionalProperties: {type: string}}
