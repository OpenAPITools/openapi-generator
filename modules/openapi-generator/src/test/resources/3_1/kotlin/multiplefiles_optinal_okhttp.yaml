openapi: 3.1.0
info:
  title: XXXXXXX
  version: latest
paths:
  /multifile/test:
    post:
      operationId: operation
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                attachments:
                  type: array
                  description: attached files
                  items:
                    type: string
                    format: binary
                pet_id:
                  type: string
                  description: Pet ID

