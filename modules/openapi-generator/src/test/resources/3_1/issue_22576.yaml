openapi: 3.1.0
info:
  title: Test
  version: "1.0"
paths:
  /test:
    post:
      summary: Test
      requestBody:
        content:
          application/json:
            schema:
              type: string
      responses:
        200:
          description: OK
        401:
          description: Unauthorized
          headers:
            WWW-Authenticate:
              $ref: "#/components/headers/WWWAuthenticate"
components:
  headers:
    WWWAuthenticate:
      required: false
      schema:
        type: string