openapi: 3.1.0
info:
  title: Enum Test
  version: 1.0.0
paths:
  /test:
    get:
      operationId: testEnum
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - in-progress
              - completed
      responses:
        '200':
          description: OK
