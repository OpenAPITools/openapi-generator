openapi: 3.1.0

info:
  title: Test
  version: "1.0"

paths:
  /a:
    post:
      summary: Test
      requestBody:
        content:
          application/json:
            schema:
              type: string
      parameters:
        - name: "arrayparam"
          in: query
          schema:
            type: array
            items:
              type: string
            default: ["test1", "test2"]
        - name: "enumarrayparam"
          in: query
          schema:
            type: array
            items:
              $ref: "#/components/schemas/ExampleEnum"
            default: ["example1"]
        - name: "stringparam"
          in: query
          schema:
            type: string
            default: "test3"
      responses:
        200:
          description: Ok
  
components:
  schemas:
    ExampleEnum:
      type: string
      enum:
        - example1
        - example2
