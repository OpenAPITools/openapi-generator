openapi: 3.1.0
info:
  version: 1.0.0
  description: Specification with integer parameters
  title: Specification with integer parameters
paths:
  /elements:
    get:
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
      - in: query
        name: offset
        schema:
          type: integer
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: boolean
