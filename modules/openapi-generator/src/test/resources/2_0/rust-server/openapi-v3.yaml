openapi: 3.0.1
info:
  title: My title
  description: API under test
  version: 1.0.7
paths:
  /xml:
    post:
      requestBody:
        content:
          application/xml:
            schema:
              $ref: '#/components/schemas/XmlArray'
      responses:
        201:
          description: OK
components:
  schemas:
    XmlArray:
      type: array
      xml:
        wrapped: true
      items:
        xml:
          name: another
        $ref: '#/components/schemas/XmlInner'
    XmlInner:
      type: string
      xml:
        name: another

