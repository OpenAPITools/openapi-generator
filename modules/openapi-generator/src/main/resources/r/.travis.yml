# ref: https://docs.travis-ci.com/user/languages/r/
language: r
cache: packages
r_packages:
- jsonlite
- httr
install:
- R -e 'devtools::install_deps(dep = T)'

script:
- R CMD build .
- R CMD check *tar.gz
