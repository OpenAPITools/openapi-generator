  # {{classname}} (OpenAPI enum). Allowed values: {{#allowableValues}}{{#enumVars}}{{{value}}}{{^-last}}, {{/-last}}{{/enumVars}}{{/allowableValues}}.
  # Represented as `{{dataType}}` so it (de)serialises transparently to/from JSON and YAML,
  # consistent with how inline enums are handled (see the `validates(..., enum: [...])` macro).
  alias {{classname}} = {{dataType}}
