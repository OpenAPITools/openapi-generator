{{> partial_header}}
function Set-OpenAPIConfiguration {

    [CmdletBinding()]
    Param(
        $BaseUrl,
        $Username,
        $Password,
        $ApiKey,
        $ApiKeyPrefix,
        $Cookie,
        $AccessToken,
        [switch]$PassThru,
        [switch]$Force
    )
    Process {

        If (!BaseUrl) {
            [void]$Script:OpenAPIConfiguration.Add('BaseUrl', $BaseUrl)
        }

    }

}
