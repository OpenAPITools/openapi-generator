{{> partial_header}}
{{#models}}
{{#model}}
function New-{{{classname}}} {
    [CmdletBinding()]
    Param (
{{#vars}}
        [Parameter(Position = {{vendorExtensions.x-index}}, ValueFromPipelineByPropertyName = $true{{#required}}, Mandatory = $true{{/required}})]
        [{{vendorExtensions.x-data-type}}]
        {{=<% %>=}}
        ${<%name%>}<%^-last%>,<%/-last%>
        <%={{ }}=%>
{{/vars}}
    )

    Process {
        #'Creating object: {{{packageName}}} => {{{classname}}}' | Write-Host
        #$PSBoundParameters | Out-DebugParameter | Write-Debug

        $PSO = [PSCustomObject]@{
            {{=<< >>=}}
            <<#vars>>
            "<<baseName>>" = ${<<name>>}
            <</vars>>
            <<={{ }}=>>
        }

        $PSO | Add-Member ScriptMethod ToString { ConvertTo-Json $this } -force

        return $PSO
    }




}
{{/model}}
{{/models}}
