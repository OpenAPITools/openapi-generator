/// [String] values for all properties defined in [{{{classname}}}].
abstract class {{{classname}}}Strings {
  const {{{classname}}}Strings._();

{{#vars}}
  static const {{{name}}}_ = "{{{baseName}}}";
{{/vars}}
}

class {{{classname}}} {
  {{{classname}}}({
  {{#vars}}
    {{#isNullable}}{{#required}}@required {{/required}}this.{{{name}}},{{/isNullable}}{{^isNullable}}{{#defaultValue}}this.{{{name}}} = {{#isContainer}}const {{/isContainer}}{{{defaultValue}}}{{/defaultValue}}{{^defaultValue}}{{#required}}@required {{/required}}this.{{{name}}}{{/defaultValue}},{{/isNullable}}
  {{/vars}}
  });

  {{#vars}}
  {{#description}}/// {{{description}}}{{/description}}
  {{^isEnum}}
  {{{dataType}}} {{{name}}};
  {{/isEnum}}
  {{#isEnum}}
  {{#allowableValues}}
  {{#min}} // range from {{{min}}} to {{{max}}}{{/min}}{{{classname}}}{{{enumName}}} {{{name}}}{{#required}} = {{{defaultValue}}}{{/required}}{{^required}}{{/required}};
  {{/allowableValues}}
  {{/isEnum}}

  {{/vars}}
  @override
  bool operator ==(Object other) => identical(this, other) || other is {{{classname}}} &&
  {{#vars}}
     other.{{{name}}} == {{{name}}}{{^-last}} &&{{/-last}}{{#-last}};{{/-last}}
  {{/vars}}

  @override
  int get hashCode =>
  {{#vars}}
    {{#isNullable}}({{{name}}}?.hashCode ?? 0){{/isNullable}}{{^isNullable}}{{{name}}}.hashCode{{/isNullable}}{{^-last}} +{{/-last}}{{#-last}};{{/-last}}
  {{/vars}}

  @override
  String toString() => _toString("");

  {{{classname}}}.fromJson(Map<String, dynamic> json) {
    if (json == null) {
      return;
    }
  {{#vars}}
  {{#isDateTime}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      DateTime.parse(json[{{{classname}}}Strings.{{{name}}}_]);
  {{/isDateTime}}
  {{#isDate}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      DateTime.parse(json[{{{classname}}}Strings.{{{name}}}_]);
  {{/isDate}}
  {{^isDateTime}}
  {{^isDate}}
    {{#complexType}}
      {{#isListContainer}}
    {{#items.isListContainer}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      (json[{{{classname}}}Strings.{{{name}}}_] as List).map(
        (e) => e == null ? null :
      {{#items.complexType}}
          {{items.complexType}}.listFromJson(json[{{{classname}}}Strings.{{{name}}}_])
      {{/items.complexType}}
      {{^items.complexType}}
          (e as List).cast<{{items.items.dataType}}>()
      {{/items.complexType}}
      ).toList();
    {{/items.isListContainer}}
    {{^items.isListContainer}}
    {{{name}}} = {{{complexType}}}.listFromJson(json[{{{classname}}}Strings.{{{name}}}_]);
    {{/items.isListContainer}}
      {{/isListContainer}}
      {{^isListContainer}}
      {{#isMapContainer}}
      {{#items.isListContainer}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      {{items.complexType}}.mapListFromJson(json[{{{classname}}}Strings.{{{name}}}_]);
      {{/items.isListContainer}}
      {{^items.isListContainer}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      {{{complexType}}}.mapFromJson(json[{{{classname}}}Strings.{{{name}}}_]);
      {{/items.isListContainer}}
      {{/isMapContainer}}
      {{^isMapContainer}}
    {{{name}}} = {{{complexType}}}.fromJson(json[{{{classname}}}Strings.{{{name}}}_]);
      {{/isMapContainer}}
      {{/isListContainer}}
    {{/complexType}}
    {{^complexType}}
      {{#isListContainer}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      (json[{{{classname}}}Strings.{{{name}}}_] as List).cast<{{{items.datatype}}}>();
      {{/isListContainer}}
      {{^isListContainer}}
        {{#isMapContainer}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      (json[{{{classname}}}Strings.{{{name}}}_] as Map).cast<String, {{{items.datatype}}}>();
        {{/isMapContainer}}
        {{^isMapContainer}}
          {{#isNumber}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_] == null ?
      null :
      json[{{{classname}}}Strings.{{{name}}}_].toDouble();
          {{/isNumber}}
          {{^isNumber}}
    {{^isEnum}}
    {{{name}}} = json[{{{classname}}}Strings.{{{name}}}_];
    {{/isEnum}}
    {{#isEnum}}
    {{{name}}} = {{{classname}}}{{{enumName}}}.fromJson(json[{{{classname}}}Strings.{{{name}}}_]);
    {{/isEnum}}
          {{/isNumber}}
        {{/isMapContainer}}
      {{/isListContainer}}
    {{/complexType}}
  {{/isDate}}
  {{/isDateTime}}
  {{/vars}}
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};

{{#vars}}
  {{#required}}
    {{>to_json}}
  {{/required}}
  {{^required}}
    {{#isNullable}}
    if ({{{name}}} != null) {
      {{>to_json}}
    }

    {{/isNullable}}
    {{^isNullable}}
    {{>to_json}}
    {{/isNullable}}
  {{/required}}
{{/vars}}
    return json;
  }

  String _toString(String prefix) {
    final sb = StringBuffer();

    sb.write("{{{classname}}}=[");

{{#vars}}
    sb.write("\n$prefix  ");
    sb.write({{{classname}}}Strings.{{{name}}}_);
    sb.write("=");
  {{#complexType}}
    {{^isContainer}}
    sb.write({{{name}}} == null ? "null" : {{{name}}}._toString("$prefix  "));
    {{/isContainer}}
    {{#isContainer}}
    sb.write({{{name}}});
    {{/isContainer}}
  {{/complexType}}
  {{^complexType}}
    sb.write({{{name}}});
  {{/complexType}}
  {{^-last}}sb.write(",");{{/-last}}

{{/vars}}
    sb.write("\n$prefix]");

    return sb.toString();
  }

  static List<{{{classname}}}> listFromJson(List<dynamic> json, {bool emptyIsNull, bool growable}) =>
    json == null || json.isEmpty
      ? true == emptyIsNull ? null : <{{{classname}}}>[]
      : json.map((v) => {{{classname}}}.fromJson(v)).toList(growable: true == growable);

  static Map<String, {{{classname}}}> mapFromJson(Map<String, dynamic> json) {
    final map = <String, {{{classname}}}>{};
    if (json != null && json.isNotEmpty) {
      json.forEach((String key, dynamic v) => map[key] = {{{classname}}}.fromJson(v));
    }
    return map;
  }

  // maps a json object with a list of {{{classname}}}-objects as value to a dart map
  static Map<String, List<{{{classname}}}>> mapListFromJson(Map<String, dynamic> json, {bool emptyIsNull, bool growable}) {
    final map = <String, List<{{{classname}}}>>{};
    if (json != null && json.isNotEmpty) {
      json.forEach((String key, dynamic v) {
        map[key] = {{{classname}}}.listFromJson(v, emptyIsNull: emptyIsNull, growable: growable);
      });
    }
    return map;
  }
}
{{#vars}}
{{#isEnum}}

{{>enum_inline}}
{{/isEnum}}
{{/vars}}
