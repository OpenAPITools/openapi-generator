    @Serializable(with = {{operationIdCamelCase}}Response.Companion::class)
    private class {{operationIdCamelCase}}Response(val value: List<{{returnBaseType}}>) {
        {{#nonPublicApi}}internal {{/nonPublicApi}}companion object : KSerializer<{{operationIdCamelCase}}Response> {
            private val serializer = ListSerializer({{returnBaseType}}.serializer())
            override val descriptor = PrimitiveSerialDescriptor("{{operationIdCamelCase}}Response", PrimitiveKind.STRING)

            override fun serialize(encoder: Encoder, value: {{operationIdCamelCase}}Response): Unit =
                serializer.serialize(encoder, value.value)

            override fun deserialize(decoder: Decoder): {{operationIdCamelCase}}Response =
                {{operationIdCamelCase}}Response(serializer.deserialize(decoder))
        }
    }
