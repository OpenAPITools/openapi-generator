import typing

from {{packageName}}.apis.tags import TagValues
{{#each tagModuleNameToApiClassname}}
from {{packageName}}.apis.tags.{{@key}} import {{this}}
{{/each}}

TagToApi = typing.TypedDict(
    'TagToApi',
    {
{{#each enumToApiClassname}}
        TagValues.{{@key}}: {{this}},
{{/each}}
    }
)

tag_to_api = TagToApi(
    {
{{#each enumToApiClassname}}
        TagValues.{{@key}}: {{this}},
{{/each}}
    }
)
