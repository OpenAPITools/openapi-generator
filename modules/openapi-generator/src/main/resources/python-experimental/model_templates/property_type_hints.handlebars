{{#if vars}}

{{#each vars}}
{{#unless nameInSnakeCase}}
{{#if complexType}}
{{baseName}}: 'MetaOapg.properties.{{baseName}}'
{{else}}
{{baseName}}: MetaOapg.properties.{{baseName}}
{{/if}}
{{/unless}}
{{/each}}
{{/if}}