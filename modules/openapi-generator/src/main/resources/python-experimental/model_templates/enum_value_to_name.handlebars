schemas.SchemaEnumMakerClsFactory(
    enum_value_to_name={
{{#if isNull}}
        schemas.NoneClass.NONE: "NONE",
{{/if}}
{{#with allowableValues}}
{{#each enumVars}}
        {{{value}}}: "{{name}}",
{{/each}}
{{/with}}
    }
),
