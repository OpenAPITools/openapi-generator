_SchemaEnumMaker(
    enum_value_to_name={
{{#if isNull}}
        NoneClass.NONE: "NONE",
{{/if}}
{{#with allowableValues}}
{{#each enumVars}}
        {{{value}}}: "{{name}}",
{{/each}}
{{/with}}
    }
),
