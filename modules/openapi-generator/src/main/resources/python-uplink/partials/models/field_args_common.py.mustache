{{#required}}required=True, {{/required}}{{#isNullable}}allow_none=True,{{/isNullable}}{{!
    ||#defaultValue||missing=|||.|||, ||/defaultValue||
    The openapi spec talks about the "consumer" providing the default.
    for deserialization, the client is consumer of responses, not requests. So, only provide default on deserialization. missing=|||defaultValue|||
    for serialization, the server should fill in the default for requests. But we could provide it client side with: default=|||defaultValue|||
    TODO: add option/switch(es) to allow serializing default values and/or deserializing missing values in client-side models (not relying on server to provide the default).
    This option will be important for specs generated by servers that don't exactly follow the rules (don't actually fill in the default).
    TODO: add a vendorExtension for spec writers to specify that default should be provided in client-side requests instead of server-side.
}}