package {{configPackage}}
{{#genericClassDef}}
/**
 * Generic class generated by openapi-generator from schema pattern '{{className}}'.
 * Type parameter [T] is the varying domain type.
 *
 * To use your own class instead, supply a fully-qualified class name via
 * `importMappings.{{className}}` in the generator config.
 */
data class {{className}}<T>(
{{#properties}}
    val {{name}}: {{kotlinType}}{{^required}}? = null{{/required}},
{{/properties}}
)
{{/genericClassDef}}
