# Schemas in fixtures.yaml whose generated C++ header does not yet exist.
# Each entry is a root-composed schema name from fixtures.yaml.
# These are DEFERRED (not FAIL) in the inventory report.
#
# When the codegen is fixed to handle these cases, remove the entry
# from this file and verify the TSV shows PASS with the expected type.
#
# NullableString — OAS 3.1 anyOf [string, null] is parsed by the swagger
# parser into {type: string, nullable: true}, which does not produce a
# standalone model header (the model type is consumed into the type system
# and works as std::optional<std::string> at reference/property level).
# This is an expected limitation of the parser/generator pipeline chain.
NullableString
