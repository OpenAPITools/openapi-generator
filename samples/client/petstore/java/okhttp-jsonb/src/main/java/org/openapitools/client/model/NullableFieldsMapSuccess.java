/*
 * OpenAPI Petstore
 * This spec is mainly for testing Petstore server and contains fake endpoints, models. Please do not use this for any other purpose. Special characters: \" \\
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import org.apache.commons.lang3.builder.EqualsBuilder;
import org.apache.commons.lang3.builder.HashCodeBuilder;
import java.util.Objects;
import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;
import org.openapitools.client.model.NullableFieldsValue;

import jakarta.json.bind.annotation.JsonbProperty;
import jakarta.json.bind.adapter.JsonbAdapter;
import jakarta.json.bind.annotation.JsonbTransient;
import jakarta.json.bind.annotation.JsonbTypeAdapter;
import jakarta.json.JsonObject;
import jakarta.json.JsonValue;
import jakarta.json.bind.Jsonb;
import jakarta.json.bind.JsonbException;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.reflect.Type;
import java.util.HashSet;

import org.openapitools.client.JSON;

/**
 * Success response containing a map of objects with nullable fields
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.26.0-SNAPSHOT")
public class NullableFieldsMapSuccess {
  public static final String SERIALIZED_NAME_STATUS = "status";
  @JsonbProperty(SERIALIZED_NAME_STATUS)
  @javax.annotation.Nonnull
  private String status;

  public static final String SERIALIZED_NAME_POSITIONS = "positions";
  @JsonbProperty(SERIALIZED_NAME_POSITIONS)
  @javax.annotation.Nonnull
  private Map<String, NullableFieldsValue> positions;

  public NullableFieldsMapSuccess() {
  }

  public NullableFieldsMapSuccess status(@javax.annotation.Nonnull String status) {
    this.status = status;
    return this;
  }

  /**
   * Get status
   * @return status
   */
  @javax.annotation.Nonnull

  public String getStatus() {
    return status;
  }

  public void setStatus(@javax.annotation.Nonnull String status) {
    this.status = status;
  }


  public NullableFieldsMapSuccess positions(@javax.annotation.Nonnull Map<String, NullableFieldsValue> positions) {
    this.positions = positions;
    return this;
  }

  public NullableFieldsMapSuccess putPositionsItem(String key, NullableFieldsValue positionsItem) {
    if (this.positions == null) {
      this.positions = new HashMap<>();
    }
    this.positions.put(key, positionsItem);
    return this;
  }

  /**
   * Get positions
   * @return positions
   */
  @javax.annotation.Nonnull

  public Map<String, NullableFieldsValue> getPositions() {
    return positions;
  }

  public void setPositions(@javax.annotation.Nonnull Map<String, NullableFieldsValue> positions) {
    this.positions = positions;
  }



  @Override
  public boolean equals(Object o) {
    return EqualsBuilder.reflectionEquals(this, o, false, null, true);
  }

  @Override
  public int hashCode() {
    return HashCodeBuilder.reflectionHashCode(this);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class NullableFieldsMapSuccess {\n");
    sb.append("    status: ").append(toIndentedString(status)).append("\n");
    sb.append("    positions: ").append(toIndentedString(positions)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("status", "positions"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("status", "positions"));
  }



  /**
   * Custom JSON-B deserializer for a model that forbids additional properties: it rejects a
   * JSON object carrying an undeclared field or missing a required one. Yasson binds such a
   * model by plain bean mapping, which silently drops an unknown key and has no notion of a
   * required property, so without this deserializer any JSON object would bind - and an
   * ambiguous {@code oneOf}/{@code anyOf} schema could not tell its branches apart. The
   * checks mirror the Gson type adapter's {@code validateJsonElement}. Registered with the
   * Jsonb instances built by {@code JSON}.
   */
  public static class CustomJsonbDeserializer implements JsonbDeserializer<NullableFieldsMapSuccess> {
    @Override
    public NullableFieldsMapSuccess deserialize(JsonParser parser, DeserializationContext context, Type rtType) {
      JsonValue value = parser.getValue();
      if (value == null || value.getValueType() == JsonValue.ValueType.NULL) {
        return null;
      }
      JsonObject jsonObj = value.asJsonObject();
      // check that the JSON object carries no field this model does not declare
      for (String field : jsonObj.keySet()) {
        if (!openapiFields.contains(field)) {
          throw new JsonbException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON object is not defined in the `NullableFieldsMapSuccess` properties. JSON: %s", field, jsonObj));
        }
      }
      for (String requiredField : openapiRequiredFields) {
        if (!jsonObj.containsKey(requiredField)) {
          throw new JsonbException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON object: %s", requiredField, jsonObj));
        }
      }
      // capture once so every field of this object binds against the same configuration,
      // even if a format setter rebuilds the shared instance concurrently
      Jsonb jsonb = JSON.getJsonb();
      NullableFieldsMapSuccess instance = new NullableFieldsMapSuccess();
      if (jsonObj.containsKey("status")) {
        instance.setStatus(jsonObj.get("status").getValueType() == JsonValue.ValueType.NULL
            ? null
            : jsonb.fromJson(jsonObj.get("status").toString(), fieldType("status")));
      }
      if (jsonObj.containsKey("positions")) {
        instance.setPositions(jsonObj.get("positions").getValueType() == JsonValue.ValueType.NULL
            ? null
            : jsonb.fromJson(jsonObj.get("positions").toString(), fieldType("positions")));
      }
      return instance;
    }

    private static java.lang.reflect.Field declaredField(String fieldName) {
      // walk up the hierarchy: inherited properties are declared on a parent class
      for (Class<?> clazz = NullableFieldsMapSuccess.class; clazz != null; clazz = clazz.getSuperclass()) {
        try {
          return clazz.getDeclaredField(fieldName);
        } catch (NoSuchFieldException e) {
          // not declared on this class; check the parent
        }
      }
      throw new IllegalArgumentException("Field " + fieldName + " not found on NullableFieldsMapSuccess");
    }

    private static Type fieldType(String fieldName) {
      return declaredField(fieldName).getGenericType();
    }

    private static void setField(NullableFieldsMapSuccess instance, String fieldName, Object value) {
      try {
        java.lang.reflect.Field field = declaredField(fieldName);
        field.setAccessible(true);
        field.set(instance, value);
      } catch (IllegalAccessException e) {
        throw new JsonbException("Unable to bind the field " + fieldName + " on NullableFieldsMapSuccess", e);
      }
    }
  }
}

