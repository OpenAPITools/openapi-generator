/*
 * OpenAPI Petstore
 * This spec is mainly for testing Petstore server and contains fake endpoints, models. Please do not use this for any other purpose. Special characters: \" \\
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import org.apache.commons.lang3.builder.EqualsBuilder;
import org.apache.commons.lang3.builder.HashCodeBuilder;
import java.util.Objects;
import java.util.Arrays;
import java.util.Map;

import jakarta.json.bind.annotation.JsonbProperty;
import jakarta.json.bind.adapter.JsonbAdapter;
import jakarta.json.bind.annotation.JsonbTransient;
import jakarta.json.bind.annotation.JsonbTypeAdapter;
import jakarta.json.JsonObject;
import jakarta.json.JsonValue;
import jakarta.json.bind.Jsonb;
import jakarta.json.bind.JsonbException;
import jakarta.json.bind.serializer.DeserializationContext;
import jakarta.json.bind.serializer.JsonbDeserializer;
import jakarta.json.stream.JsonParser;
import java.lang.reflect.Type;
import java.util.HashSet;

import org.openapitools.client.JSON;

/**
 * IsoscelesTriangle
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.26.0-SNAPSHOT")
public class IsoscelesTriangle {
  public static final String SERIALIZED_NAME_SHAPE_TYPE = "shapeType";
  @JsonbProperty(SERIALIZED_NAME_SHAPE_TYPE)
  @javax.annotation.Nonnull
  private String shapeType;

  public static final String SERIALIZED_NAME_TRIANGLE_TYPE = "triangleType";
  @JsonbProperty(SERIALIZED_NAME_TRIANGLE_TYPE)
  @javax.annotation.Nonnull
  private String triangleType;

  public IsoscelesTriangle() {
  }

  public IsoscelesTriangle shapeType(@javax.annotation.Nonnull String shapeType) {
    this.shapeType = shapeType;
    return this;
  }

  /**
   * Get shapeType
   * @return shapeType
   */
  @javax.annotation.Nonnull

  public String getShapeType() {
    return shapeType;
  }

  public void setShapeType(@javax.annotation.Nonnull String shapeType) {
    this.shapeType = shapeType;
  }


  public IsoscelesTriangle triangleType(@javax.annotation.Nonnull String triangleType) {
    this.triangleType = triangleType;
    return this;
  }

  /**
   * Get triangleType
   * @return triangleType
   */
  @javax.annotation.Nonnull

  public String getTriangleType() {
    return triangleType;
  }

  public void setTriangleType(@javax.annotation.Nonnull String triangleType) {
    this.triangleType = triangleType;
  }



  @Override
  public boolean equals(Object o) {
    return EqualsBuilder.reflectionEquals(this, o, false, null, true);
  }

  @Override
  public int hashCode() {
    return HashCodeBuilder.reflectionHashCode(this);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class IsoscelesTriangle {\n");
    sb.append("    shapeType: ").append(toIndentedString(shapeType)).append("\n");
    sb.append("    triangleType: ").append(toIndentedString(triangleType)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("shapeType", "triangleType"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("shapeType", "triangleType"));
  }



  /**
   * Custom JSON-B deserializer for a model that forbids additional properties: it rejects a
   * JSON object carrying an undeclared field or missing a required one. Yasson binds such a
   * model by plain bean mapping, which silently drops an unknown key and has no notion of a
   * required property, so without this deserializer any JSON object would bind - and an
   * ambiguous {@code oneOf}/{@code anyOf} schema could not tell its branches apart. The
   * checks mirror the Gson type adapter's {@code validateJsonElement}. Registered with the
   * Jsonb instances built by {@code JSON}.
   */
  public static class CustomJsonbDeserializer implements JsonbDeserializer<IsoscelesTriangle> {
    @Override
    public IsoscelesTriangle deserialize(JsonParser parser, DeserializationContext context, Type rtType) {
      JsonValue value = parser.getValue();
      if (value == null || value.getValueType() == JsonValue.ValueType.NULL) {
        return null;
      }
      JsonObject jsonObj = value.asJsonObject();
      // check that the JSON object carries no field this model does not declare
      for (String field : jsonObj.keySet()) {
        if (!openapiFields.contains(field)) {
          throw new JsonbException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON object is not defined in the `IsoscelesTriangle` properties. JSON: %s", field, jsonObj));
        }
      }
      for (String requiredField : openapiRequiredFields) {
        if (!jsonObj.containsKey(requiredField)) {
          throw new JsonbException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON object: %s", requiredField, jsonObj));
        }
      }
      // capture once so every field of this object binds against the same configuration,
      // even if a format setter rebuilds the shared instance concurrently
      Jsonb jsonb = JSON.getJsonb();
      IsoscelesTriangle instance = new IsoscelesTriangle();
      if (jsonObj.containsKey("shapeType")) {
        instance.setShapeType(jsonObj.get("shapeType").getValueType() == JsonValue.ValueType.NULL
            ? null
            : jsonb.fromJson(jsonObj.get("shapeType").toString(), fieldType("shapeType")));
      }
      if (jsonObj.containsKey("triangleType")) {
        instance.setTriangleType(jsonObj.get("triangleType").getValueType() == JsonValue.ValueType.NULL
            ? null
            : jsonb.fromJson(jsonObj.get("triangleType").toString(), fieldType("triangleType")));
      }
      return instance;
    }

    private static java.lang.reflect.Field declaredField(String fieldName) {
      // walk up the hierarchy: inherited properties are declared on a parent class
      for (Class<?> clazz = IsoscelesTriangle.class; clazz != null; clazz = clazz.getSuperclass()) {
        try {
          return clazz.getDeclaredField(fieldName);
        } catch (NoSuchFieldException e) {
          // not declared on this class; check the parent
        }
      }
      throw new IllegalArgumentException("Field " + fieldName + " not found on IsoscelesTriangle");
    }

    private static Type fieldType(String fieldName) {
      return declaredField(fieldName).getGenericType();
    }

    private static void setField(IsoscelesTriangle instance, String fieldName, Object value) {
      try {
        java.lang.reflect.Field field = declaredField(fieldName);
        field.setAccessible(true);
        field.set(instance, value);
      } catch (IllegalAccessException e) {
        throw new JsonbException("Unable to bind the field " + fieldName + " on IsoscelesTriangle", e);
      }
    }
  }
}

