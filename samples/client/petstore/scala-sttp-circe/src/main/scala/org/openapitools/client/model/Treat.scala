/**
 * OpenAPI Petstore
 * This is a sample server Petstore server. For this sample, you can use the api key `special-key` to test the authorization filters.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.model

import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import org.openapitools.client.core.JsonSupport._

sealed trait Treat
object Treat {
  implicit val encoderTreat: Encoder[Treat] = Encoder.instance {
    case obj: DryFood => obj.asJson.mapObject(("treatType" -> "DryFood".asJson) +: _)
    case obj: WetFood => obj.asJson.mapObject(("treatType" -> "WetFood".asJson) +: _)
  }
  implicit val decoderTreat: Decoder[Treat] = Decoder.instance { c =>
    c.downField("treatType").as[String].flatMap {
      case "DryFood" => c.as[DryFood]
      case "WetFood" => c.as[WetFood]
      case other => Left(DecodingFailure(s"Unknown discriminator value: $other", c.history))
    }
  }
}

case class DryFood(
  treatType: String,
  kibbleSize: Double
) extends Treat
object DryFood {
  implicit val encoderDryFood: Encoder[DryFood] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        Some("treatType" -> t.treatType.asJson),
        Some("kibbleSize" -> t.kibbleSize.asJson)
      ).flatten
    }
  }
  implicit val decoderDryFood: Decoder[DryFood] = Decoder.instance { c =>
    for {
      treatType <- c.downField("treatType").as[String]
      kibbleSize <- c.downField("kibbleSize").as[Double]
    } yield DryFood(
      treatType = treatType,
      kibbleSize = kibbleSize
    )
  }
}

case class WetFood(
  treatType: String,
  canSize: String
) extends Treat
object WetFood {
  implicit val encoderWetFood: Encoder[WetFood] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        Some("treatType" -> t.treatType.asJson),
        Some("canSize" -> t.canSize.asJson)
      ).flatten
    }
  }
  implicit val decoderWetFood: Decoder[WetFood] = Decoder.instance { c =>
    for {
      treatType <- c.downField("treatType").as[String]
      canSize <- c.downField("canSize").as[String]
    } yield WetFood(
      treatType = treatType,
      canSize = canSize
    )
  }
}


