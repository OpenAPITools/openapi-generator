/**
 * OpenAPI Petstore
 * This is a sample server Petstore server. For this sample, you can use the api key `special-key` to test the authorization filters.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0-SNAPSHOT.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "Category.h"

#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

Category::Category(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string Category::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void Category::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value Category::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void Category::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for Category");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object Category::toJsonObject_internal() const
{
    boost::json::object object;
    object["id"] = m_Id;
    object["name"] = m_Name;
    return object;
}

void Category::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto IdIt = object.find("id");
        if (IdIt != object.end()) {
            m_Id = boost::json::value_to<int64_t>(IdIt->value());
        }
    }
    {
        const auto NameIt = object.find("name");
        if (NameIt != object.end()) {
            m_Name = boost::json::value_to<std::string>(NameIt->value());
        }
    }
}

int64_t Category::getId() const
{
    return m_Id;
}

void Category::setId(int64_t value)
{
    m_Id = value;
}
std::string Category::getName() const
{
    return m_Name;
}

void Category::setName(std::string value)
{
    m_Name = value;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<Category>>& data)
{
    boost::json::array array;
    for (const auto& item : data) {
        array.emplace_back(item == nullptr ? boost::json::value(nullptr) : item->toJsonValue());
    }
    return boost::json::serialize(array);
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<Category>>& vec, const std::string& json)
{
    const auto array = boost::json::parse(json).as_array();
    for (const auto& item : array) {
        if (!item.is_null()) {
            vec.emplace_back(std::make_shared<Category>(item));
        }
    }
}

}
}
}
}

