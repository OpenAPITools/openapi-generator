/**
 * OpenAPI Petstore
 * This spec is mainly for testing Petstore server and contains fake endpoints, models. Please do not use this for any other purpose. Special characters: \" \\
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Salmon from './Salmon';
import Tuna from './Tuna';

/**
 * The Fish model module.
 * @module model/Fish
 * @version 1.0.0
 */
class Fish {
    /**
     * Constructs a new <code>Fish</code>.
     * @alias module:model/Fish
     * @param {(module:model/Salmon|module:model/Tuna)} instance The actual instance to initialize Fish.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        // if discriminator exists, use it.
        if (instance && typeof instance === 'object' && instance['fishType'] !== undefined) {
            var discriminatorValue = instance['fishType'];
            switch (discriminatorValue) {
                case 'atlantic_salmon':
                    try {
                        Salmon.validateJSON(instance);
                        this.actualInstance = Salmon.constructFromObject(instance);
                        return;
                    } catch(err) {
                        throw new Error("Failed to construct Salmon using discriminator value 'atlantic_salmon': " + err);
                    }
                case 'yellowfin_tuna':
                    try {
                        Tuna.validateJSON(instance);
                        this.actualInstance = Tuna.constructFromObject(instance);
                        return;
                    } catch(err) {
                        throw new Error("Failed to construct Tuna using discriminator value 'yellowfin_tuna': " + err);
                    }
                default:
                    throw new Error("Unrecognized discriminator value '" + discriminatorValue + "' constructing `Fish`. Expected one of: atlantic_salmon, yellowfin_tuna.");
            }
        }
        // else try to construct using oneOf

        try {
            if (instance instanceof Salmon) {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                Salmon.validateJSON(instance); // throw an exception if no match
                // create Salmon from JS object
                this.actualInstance = Salmon.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into Salmon
            errorMessages.push("Failed to construct Salmon: " + err)
        }

        try {
            if (instance instanceof Tuna) {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                Tuna.validateJSON(instance); // throw an exception if no match
                // create Tuna from JS object
                this.actualInstance = Tuna.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into Tuna
            errorMessages.push("Failed to construct Tuna: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `Fish` with oneOf schemas Salmon, Tuna. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `Fish` with oneOf schemas Salmon, Tuna. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>Fish</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Fish} obj Optional instance to populate.
     * @return {module:model/Fish} The populated <code>Fish</code> instance.
     */
    static constructFromObject(data, obj) {
        return new Fish(data);
    }

    /**
     * Gets the actual instance, which can be <code>Salmon</code>, <code>Tuna</code>.
     * @return {(module:model/Salmon|module:model/Tuna)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>Salmon</code>, <code>Tuna</code>.
     * @param {(module:model/Salmon|module:model/Tuna)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = Fish.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of Fish from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/Fish} An instance of Fish.
     */
    static fromJSON = function(json_string){
        return Fish.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * @member {String} fishType
 */
Fish.prototype['fishType'] = undefined;

/**
 * @member {Number} length
 */
Fish.prototype['length'] = undefined;

/**
 * @member {Number} weight
 */
Fish.prototype['weight'] = undefined;


Fish.OneOf = ["Salmon", "Tuna"];

export default Fish;

