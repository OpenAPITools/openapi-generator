#
# OpenAPI Petstore
# 
# This is a sample server Petstore server. For this sample, you can use the api key `special-key` to test the authorization filters.
# The version of the OpenAPI document: 1.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PetAlertLevel* {.pure.} = enum
  `0`
  `1`
  `2`
  `10`
  `99`

func `%`*(v: PetAlertLevel): JsonNode =
  result = case v:
    of PetAlertLevel.`0`: %(0)
    of PetAlertLevel.`1`: %(1)
    of PetAlertLevel.`2`: %(2)
    of PetAlertLevel.`10`: %(10)
    of PetAlertLevel.`99`: %(99)

func `$`*(v: PetAlertLevel): string =
  result = case v:
    of PetAlertLevel.`0`: $(0)
    of PetAlertLevel.`1`: $(1)
    of PetAlertLevel.`2`: $(2)
    of PetAlertLevel.`10`: $(10)
    of PetAlertLevel.`99`: $(99)
proc to*(node: JsonNode, T: typedesc[PetAlertLevel]): PetAlertLevel =
  if node.kind != JInt:
    raise newException(ValueError, "Expected integer for enum PetAlertLevel, got " & $node.kind)
  let intVal = node.getInt()
  case intVal:
  of 0:
    return PetAlertLevel.`0`
  of 1:
    return PetAlertLevel.`1`
  of 2:
    return PetAlertLevel.`2`
  of 10:
    return PetAlertLevel.`10`
  of 99:
    return PetAlertLevel.`99`
  else:
    raise newException(ValueError, "Invalid enum value for PetAlertLevel: " & $intVal)

