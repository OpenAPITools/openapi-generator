#
# OpenAPI Petstore
# This spec is mainly for testing Petstore server and contains fake endpoints, models. Please do not use this for any other purpose. Special characters: "" \
# Version: 1.0.0
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

Quadrilateral<PSCustomObject>
#>
function ConvertFrom-PSJsonToQuadrilateral {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match ComplexQuadrilateral defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-PSJsonToComplexQuadrilateral $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "ComplexQuadrilateral"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'ComplexQuadrilateral' defined in oneOf (PSQuadrilateral). Proceeding to the next one if any."
        }

        # try to match SimpleQuadrilateral defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-PSJsonToSimpleQuadrilateral $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SimpleQuadrilateral"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SimpleQuadrilateral' defined in oneOf (PSQuadrilateral). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([ComplexQuadrilateral, SimpleQuadrilateral]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("ComplexQuadrilateral", "SimpleQuadrilateral")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([ComplexQuadrilateral, SimpleQuadrilateral]). JSON Payload: $($Json)"
        }
    }
}

