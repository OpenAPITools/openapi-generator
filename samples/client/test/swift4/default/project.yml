name: TestClient
targets:
  TestClient:
    type: framework
    platform: iOS
    deploymentTarget: "10.0"
    sources: [TestClient]
    info:
      path: ./Info.plist
    settings:
      APPLICATION_EXTENSION_API_ONLY: true
    scheme: {}