openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
servers:
- url: /
paths: {}
components:
  schemas:
    foo:
      properties:
        bar:
          default: value1
          enum:
          - value1
          type: string
        baz:
          default: value2
          type: string
        qux:
          type: string
        thud:
          default: 42
          type: integer
      required:
      - bar
      - baz
      - qux
      type: object
