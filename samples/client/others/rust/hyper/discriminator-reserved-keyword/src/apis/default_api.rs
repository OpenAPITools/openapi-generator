/*
 * Rust Discriminator Reserved Keyword Test
 *
 * This tests discriminator fields with Rust reserved keywords to ensure proper escaping and function name generation 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use std::sync::Arc;
use std::borrow::Borrow;
use std::pin::Pin;
#[allow(unused_imports)]
use std::option::Option;

use hyper;
use hyper_util::client::legacy::connect::Connect;
use futures::Future;

use crate::models;
use super::{Error, configuration};
use super::request as __internal_request;

pub struct DefaultApiClient<C: Connect>
    where C: Clone + std::marker::Send + Sync + 'static {
    configuration: Arc<configuration::Configuration<C>>,
}

impl<C: Connect> DefaultApiClient<C>
    where C: Clone + std::marker::Send + Sync {
    pub fn new(configuration: Arc<configuration::Configuration<C>>) -> DefaultApiClient<C> {
        DefaultApiClient {
            configuration,
        }
    }
}

pub trait DefaultApi: Send + Sync {
    fn create_shape(&self, shape: models::Shape) -> Pin<Box<dyn Future<Output = Result<models::Shape, Error>> + Send>>;
    fn create_vehicle(&self, vehicle: models::Vehicle) -> Pin<Box<dyn Future<Output = Result<models::Vehicle, Error>> + Send>>;
}

impl<C: Connect>DefaultApi for DefaultApiClient<C>
    where C: Clone + std::marker::Send + Sync {
    #[allow(unused_mut)]
    fn create_shape(&self, shape: models::Shape) -> Pin<Box<dyn Future<Output = Result<models::Shape, Error>> + Send>> {
        let mut req = __internal_request::Request::new(hyper::Method::POST, "/shape".to_string())
        ;
        req = req.with_body_param(shape);

        req.execute(self.configuration.borrow())
    }

    #[allow(unused_mut)]
    fn create_vehicle(&self, vehicle: models::Vehicle) -> Pin<Box<dyn Future<Output = Result<models::Vehicle, Error>> + Send>> {
        let mut req = __internal_request::Request::new(hyper::Method::POST, "/vehicle".to_string())
        ;
        req = req.with_body_param(vehicle);

        req.execute(self.configuration.borrow())
    }

}
