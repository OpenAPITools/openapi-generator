# #Qdrant API
#
#The version of the OpenAPI document: master
#Contact: andrey@vasnetsov.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.25.0-SNAPSHOT
#

module Qdrant::Api
  # Use context and a target to find the most similar points, constrained by the context.
  class DiscoverRequest
    include JSON::Serializable
    include YAML::Serializable
    include Qdrant::Api::Serializable
    include Qdrant::Api::Validation

    # Required properties
    # Max number of result to return
    @[JSON::Field(key: "limit", emit_null: false)]
    property limit : Int32

    # Optional properties
    # Specify in which shards to look for the points, if not specified - look in all shards
    @[JSON::Field(key: "shard_key", emit_null: false)]
    property shard_key : ShardKeySelector?

    # Look for vectors closest to this.  When using the target (with or without context), the integer part of the score represents the rank with respect to the context, while the decimal part of the score relates to the distance to the target.
    @[JSON::Field(key: "target", emit_null: false)]
    property target : RecommendExample?

    # Pairs of { positive, negative } examples to constrain the search.  When using only the context (without a target), a special search - called context search - is performed where pairs of points are used to generate a loss that guides the search towards the zone where most positive examples overlap. This means that the score minimizes the scenario of finding a point closer to a negative than to a positive part of a pair.  Since the score of a context relates to loss, the maximum score a point can get is 0.0, and it becomes normal that many points can have a score of 0.0.  For discovery search (when including a target), the context part of the score for each pair is calculated +1 if the point is closer to a positive than to a negative part of a pair, and -1 otherwise.
    @[JSON::Field(key: "context", emit_null: false)]
    property context : Array(ContextExamplePair)?

    # Look only for points which satisfies this conditions
    @[JSON::Field(key: "filter", emit_null: false)]
    property filter : Filter?

    # Additional search params
    @[JSON::Field(key: "params", emit_null: false)]
    property params : SearchParams?

    # Offset of the first result to return. May be used to paginate results. Note: large offset values may cause performance issues.
    @[JSON::Field(key: "offset", emit_null: false)]
    property offset : Int32?

    # Select which payload to return with the response. Default is false.
    @[JSON::Field(key: "with_payload", emit_null: false)]
    property with_payload : WithPayloadInterface?

    # Options for specifying which vectors to include into response. Default is false.
    @[JSON::Field(key: "with_vector", emit_null: false)]
    property with_vector : WithVector?

    # Define which vector to use for recommendation, if not specified - try to use default vector
    @[JSON::Field(key: "using", emit_null: false)]
    property using : String?

    # The location used to lookup vectors. If not specified - use current collection. Note: the other collection should have the same vector size as the current collection
    @[JSON::Field(key: "lookup_from", emit_null: false)]
    property lookup_from : LookupLocation?

    validates(limit, Int32, false, minimum: 1)
    validates(offset, Int32, true, minimum: 0)

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@limit : Int32, @shard_key : ShardKeySelector? = nil, @target : RecommendExample? = nil, @context : Array(ContextExamplePair)? = nil, @filter : Filter? = nil, @params : SearchParams? = nil, @offset : Int32? = nil, @with_payload : WithPayloadInterface? = nil, @with_vector : WithVector? = nil, @using : String? = nil, @lookup_from : LookupLocation? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if (msg = limit_validation_error(@limit))
        invalid_properties.push(msg)
      end
      if (msg = offset_validation_error(@offset))
        invalid_properties.push(msg)
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      list_invalid_properties.empty?
    end

    def_equals_and_hash(shard_key, target, context, filter, params, limit, offset, with_payload, with_vector, using, lookup_from)
  end

end
