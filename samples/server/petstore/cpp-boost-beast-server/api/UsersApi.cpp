/**
 * Petstore Server
 * Sample petstore server for the cpp-boost-beast-server generator
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.26.0-SNAPSHOT.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * UsersApi.cpp
 */

#include "UsersApi.h"

#include "server/BodyJson.h"
#include "server/ParamCodecs.h"
#include "server/Problem.h"
#include "server/Responder.h"
#include "server/Router.h"

#include <algorithm>
#include <cctype>
#include <regex>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

namespace org {
namespace openapitools {
namespace server {
namespace api {

void UsersApi::attach(HttpServer& server, std::shared_ptr<UsersApi> impl) {
    auto router = server.routerPtr();

    // ------------------------------------------------------------------
    // GET /users/{username} (getUserByName)
    // ------------------------------------------------------------------
    {
        SecurityGroups security;

        router->add(
            "GET",
            "/users/{username}",
            [impl](std::shared_ptr<RequestContext> ctx,
                    std::shared_ptr<ResponderCore> responderCore) {
                GetUserByNameRequest request;
                Problem problem;
                bool invalid = false;

                // ---- parameter username (path, simple) ----
                {
                    auto rawSegment = ctx->pathParams.find("username");
                    std::string encoded =
                        rawSegment != ctx->pathParams.end() ? rawSegment->second : std::string();
                    bool malformed = false;
                    std::string text;
                    text = percentDecode(encoded);
                    if (malformed) {
                        problem.withError("username", "path parameter is not simple-encoded");
                        invalid = true;
                    } else if (text.empty()) {
                        problem.withError("username", "path parameter is missing or empty");
                        invalid = true;
                    } else if (!parseScalar(text, request.username)) {
                        problem.withError("username", "path parameter is not a valid std::string");
                        invalid = true;
                    }




                }


                if (invalid) {
                    problem.status = 400;
                    problem.title = "Bad Request";
                    responderCore->sendProblem(std::move(problem));
                    return;
                }

                GetUserByNameResponder responder(responderCore);
                impl->getUserByName(std::move(request), ctx, std::move(responder));
            },
            std::move(security),
            "getUserByName");
    }
}

}
}
}
}
