/**
 * Petstore Server
 * Sample petstore server for the cpp-boost-beast-server generator
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.26.0-SNAPSHOT.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

// ============================================================================
// main.cpp - quick-start server entry point (generated with
// addApiImplStubs=true). Every operation answers 501 problem+json until you
// provide a real service implementation.
// ============================================================================
#include <boost/asio.hpp>

#include <cstdlib>
#include <iostream>
#include <memory>

#include "HttpServer.h"
#include "Router.h"

#include "PetsApi.h"
#include "StoreApi.h"
#include "UsersApi.h"
using namespace org;
using namespace openapitools;
using namespace server;
using namespace api;

static void attachPetsApi(HttpServer& server) {
    PetsApi::attach(server, std::make_shared<PetsApiStub>());
}
static void attachStoreApi(HttpServer& server) {
    StoreApi::attach(server, std::make_shared<StoreApiStub>());
}
static void attachUsersApi(HttpServer& server) {
    UsersApi::attach(server, std::make_shared<UsersApiStub>());
}

int main() {
    unsigned port = 8080;
    if (char const* portText = std::getenv("PORT")) {
        port = static_cast<unsigned>(std::strtoul(portText, nullptr, 10));
    }

    try {
        boost::asio::io_context ioc;
        auto router = std::make_shared<Router>();
        auto server = std::make_shared<HttpServer>(ioc, router);
        attachPetsApi(*server);
        attachStoreApi(*server);
        attachUsersApi(*server);
        server->listen(boost::asio::ip::tcp::endpoint{
            boost::asio::ip::make_address("0.0.0.0"),
            static_cast<unsigned short>(port)});
        std::cout << "CppBoostBeastPetstoreServer listening on 0.0.0.0:" << port << "\n";
        ioc.run();
    } catch (std::exception const& error) {
        std::cerr << "fatal: " << error.what() << "\n";
        return EXIT_FAILURE;
    }
    return EXIT_SUCCESS;
}
