/*
 * Generated by OpenAPI Generator, do not edit.
 * https://openapi-generator.tech
 */

#include "Order.h"

namespace models {

Order::Order()
: m_id(0),
 m_petId(0),
 m_quantity(0),
 m_shipDate(""),
 m_status(""),
 m_complete(false)
{
}

nlohmann::json Order::toJson(const Order& obj)
{
    nlohmann::json json;
    json["id"] = obj.getId();
    json["petId"] = obj.getPetId();
    json["quantity"] = obj.getQuantity();
    json["shipDate"] = obj.getShipDate();
    json["status"] = obj.getStatus();
    json["complete"] = obj.isComplete();
    return json;
}

Order Order::fromJson(const nlohmann::json& json)
{
    Order obj;
    if (json.contains("id"))
    {
        obj.setId(json.at("id").get<long>());
    }
    if (json.contains("petId"))
    {
        obj.setPetId(json.at("petId").get<long>());
    }
    if (json.contains("quantity"))
    {
        obj.setQuantity(json.at("quantity"));
    }
    if (json.contains("shipDate"))
    {
        obj.setShipDate(json.at("shipDate"));
    }
    if (json.contains("status"))
    {
        obj.setStatus(json.at("status"));
    }
    if (json.contains("complete"))
    {
        obj.setComplete(json.at("complete"));
    }
    return obj;
}


long Order::getId() const
{
    return m_id;
}

void Order::setId(const long& id)
{
    m_id = id;
}
long Order::getPetId() const
{
    return m_petId;
}

void Order::setPetId(const long& petId)
{
    m_petId = petId;
}
int Order::getQuantity() const
{
    return m_quantity;
}

void Order::setQuantity(const int& quantity)
{
    m_quantity = quantity;
}
Date Order::getShipDate() const
{
    return m_shipDate;
}

void Order::setShipDate(const Date& shipDate)
{
    m_shipDate = shipDate;
}
StatusEnum Order::getStatus() const
{
    return m_status;
}

void Order::setStatus(const StatusEnum& status)
{
    m_status = status;
}
bool Order::isComplete() const
{
    return m_complete;
}

void Order::setComplete(const bool& complete)
{
    m_complete = complete;
}

} // namespace models
